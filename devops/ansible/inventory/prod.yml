---
# EXAMPLE inventory. Replace host names and addresses before use.
all:
  children:
    tbd:
      hosts:
        prod-1:
          ansible_host: 203.0.113.20   # EXAMPLE (TEST-NET-3), replace
          ansible_user: root
      vars:
        env_name: prod
        # Always pass an explicit, immutable tag on the command line: -e image_tag=v1.2.3
        image_tag: REPLACE_ME
