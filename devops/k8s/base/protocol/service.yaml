apiVersion: v1
kind: Service
metadata:
  name: protocol
  labels:
    app.kubernetes.io/name: protocol
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: protocol
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
      appProtocol: http
