# finance, environment "local": nothing differs from base.toml yet. The file
# must exist so a mistyped TBD_ENV fails at start instead of running on base.

[mail]
# A local send reaches only the owner's own addresses, and the accountant --
# the one outside address the month's bundle is for. Anyone else is still
# refused before the provider is asked.
allow_to = ["nevio@inorbit.hr", "nevio.vesic@gmail.com", "racunovodstvo.trinajstic@ri.t-com.hr"]
