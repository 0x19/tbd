# ledger, environment "local": the k3d cluster and the compose stack, with a
# small pool and a fast outbox and sweeper so changes show up quickly.

[store]
max_connections = 4

[analytics]
period = "250ms"

[erasure]
sweep_interval = "1m"
