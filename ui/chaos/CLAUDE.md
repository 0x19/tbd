@AGENTS.md

# ui/chaos

The chaos admin UI. Read `docs/chaos/ui.md` first; `docs/chaos/api.md` is the contract
the pages consume.

- Static export (`output: "export"`, `basePath: "/chaos"`, `trailingSlash: true`).
  Every page is `"use client"` and fetches from the API; nothing here may need a Node
  runtime (no route handlers, server actions, dynamic segments, `next/image` loader).
- The API base is same-origin in a build and `http://127.0.0.1:7700/api/chaos` under
  `next dev` (`NEXT_PUBLIC_CHAOS_API` overrides). Do not hardcode hosts in pages.
- `src/lib/api/schema.ts` mirrors `crates/chaos/src/api` one type at a time through Zod.
  Change both, and `docs/chaos/api.md`, in the same commit.
- `src/components/ui/` is generated by `pnpm dlx shadcn@latest add <name>`; this shadcn
  version builds on Base UI, so composition is `render={<Link href=... />}`, not
  `asChild`. Regenerate rather than hand-edit.
- ESLint runs the React Compiler rules: no `setState` in effect bodies, no ref access
  during render. `useRunFeed` in `src/lib/api/hooks.ts` shows how to reset per id
  without an effect.
- Detail pages are `/x/view/?id=` with `useSearchParams` under `Suspense`; a dynamic
  segment cannot be exported.
- `pnpm dev` is on 3001 because Grafana owns 3000 locally.
- Checks: `mise run ui:check` (prettier, eslint, tsc) is part of `mise run ci`; `mise run
  ui:build` must pass before the chaos image is built (`local:build` does both).
- `e2e/smoke.mjs` (`mise run ui:e2e`) is the browser check against a running cluster;
  extend it when a page gains a flow. Locators must use `.first()` where a toast repeats
  table text.
- `chaos serve` serves `out/` through its own handler (`crates/chaos/src/api/ui.rs`):
  a directory serves its `index.html` without a redirect, unknown paths get `404.html`.
