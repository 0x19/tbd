/// The design system: the web kit's tokens as a Flutter theme.
///
/// `tokens.g.dart` is generated from `ui/finances/src/app/globals.css` by
/// `mobile/tool/theme.dart`; `TbdTheme` turns it into Material and
/// `TbdColors` keeps the kit's own names for what Material has no word for.
library;

export 'src/theme.dart';
export 'src/tokens.g.dart';
